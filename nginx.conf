events{}
http {
    include /etc/nginx/mime.types;
    server {
        listen 5173;
        server_name https://gymlink-frontend-cweybgrrnq-uc.a.run.app;
        root /usr/share/nginx/html;
        index index.html;
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}